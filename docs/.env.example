# copy to .env (.env file is gitignored)
# optional. jekyll-remote-theme hits the GitHub API and is rate limited to 60/hour without a token
# generate one at https://github.com/settings/tokens/new with the scope `public_repo`
JEKYLL_GITHUB_TOKEN=
